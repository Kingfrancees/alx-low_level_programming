#include <stdio.h>
/**
 * main - Entry point
 * description: c programming for life
 * Return: (0) SUCCESS
 */

int main(void)
{
	char x;

	for (x = 'z'; x >= 'a'; x--)
	putchar(x);
	putchar('\n');
	return (0);
}
